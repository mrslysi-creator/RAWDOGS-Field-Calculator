# Builds

Published binaries should normally be downloaded from **GitHub Releases** rather than committed repeatedly into the main source history.

This directory can contain build notes, packaging scripts or small supporting files.

Current stable Windows reference: **Build 2.4**.
